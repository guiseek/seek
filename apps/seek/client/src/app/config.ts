import { WebConfig } from '@guiseek/web-router'

export const config: WebConfig = {
  templateId: '#web-layout',
  pageSelector: 'web-page',
  itemSelector: 'li',
  activeClass: 'active',
  logColor: '#33A2F8',
  theme: {
    lightClass: 'light',
    darkClass: 'dark',
  },
}
